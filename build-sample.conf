# ↓ 应用基础信息配置
export APP_NAME="matterlace"                                # 设备上显示的应用名称
export APP_SCHEME="mattermost-livelace"                     # Android URI协议方案（用于深度链接）
export MAIN_APP_IDENTIFIER="ru.livelace.mattermost"         # Android应用ID（包名）
export SUPPLY_PACKAGE_NAME="ru.livelace.mattermost"         # 应用商店包名（需与MAIN_APP_IDENTIFIER相同）

# ↓ 版本控制配置
export BRANCH_TO_BUILD="release-1.55"                       # 要构建的Git分支/版本标签

# ↓ 构建类型配置
export BUILD_FOR_RELEASE="true"                             # 是否构建发布版本（非调试版）
export REPLACE_ASSETS="true"                                # 是否使用定制资源（图标/图片等）

# ↓ APK签名配置（需替换为实际值）
export MATTERMOST_RELEASE_KEY_ALIAS="<KEY_ALIAS>"           # 密钥库中的密钥别名
export MATTERMOST_RELEASE_PASSWORD="<KEYSTORE_PASSWORD>"    # 密钥库密码
